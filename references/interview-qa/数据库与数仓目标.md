### 数据中台 + 数据治理工程师必备 SQL 技能点

1. **基础 DML**：增删改查（SELECT / INSERT / UPDATE / DELETE）、WHERE / GROUP BY / ORDER BY / HAVING
2. **多表关联**：INNER / LEFT / RIGHT / FULL JOIN，关联条件与关联键设计
3. **子查询与 CTE**：标量子查询、关联子查询、WITH 子句
4. **窗口函数**：ROW_NUMBER / RANK / DENSE_RANK / SUM/AVG/COUNT() OVER()
5. **聚合与高级函数**：COUNT (DISTINCT)、CASE WHEN、COALESCE、NVL、IFNULL、日期 / 字符串 / 数值函数
6. **Hive SQL 核心**：分区、分桶、ORC/Parquet、UDF、侧视图（LATERAL VIEW）
7. **数据建模 SQL**：ODS/DWD/DWS/ADS 分层建表、拉链表、快照表、缓慢渐变维（SCD）
8. **数据治理相关**：数据血缘、字段血缘查询、元数据统计、数据质量校验 SQL
9. **数据质量规则**：空值校验、唯一性校验、范围校验、重复数据排查
10. **性能优化**：执行计划、索引、分区裁剪、避免全表扫描、数据倾斜处理
11. **数据同步与集成**：INSERT OVERWRITE / INSERT INTO、数据去重、增量同步 SQL
12. **权限与安全**：GRANT / REVOKE、行权限、列权限、脱敏函数（掩码、加密）
13. **数仓标准语法**：建表语句、字段规范、注释、约束、分区表设计
---

### 数据中台 + 数据治理工程师必备 Hive 技能点

1. Hive 架构与核心组件（Metastore、Driver、Execution Engine）
2. Hive 数据类型、存储格式（Text/ORC/Parquet）与压缩
3. 内部表、外部表、分区表、分桶表、视图、物化视图
4. Hive DDL/DML 完整语法（建库建表、增删改查、分区管理）
5. Hive SQL 优化（谓词下推、分区裁剪、MapJoin、并行执行、小文件处理）
6. Hive 函数（内置函数、UDF/UDAF/UDTF 开发与使用）
7. Hive 数据倾斜定位与解决
8. Hive 与 HDFS、YARN、Spark 集成
9. Hive 数据导入导出（LOAD、INSERT、Sqoop/DataX 对接）
10. Hive 权限管理与安全（RBAC、行 / 列级权限、Kerberos）
11. Hive 事务与 ACID 表（更新删除、增量数据处理）
12. Hive 数仓分层建模（ODS/DWD/DWS/DWT/ADS）
13. Hive 元数据管理（元数据查询、血缘分析、影响分析）
14. Hive 数据质量监控（空值、重复、一致性、规则校验）
15. Hive 调度与任务依赖（配合 Airflow/DolphinScheduler/DataWorks）
16. Hive 常见问题排查（执行慢、内存溢出、数据丢失、元数据不一致）
---

### 数据中台 / 数据治理工程师必备 Milvus 技能点

1. 核心概念与数据模型：Database、Collection、Partition、Segment、Field、Entity、Schema 定义
2. 向量与标量混合建模：向量维度、距离度量（L2/IP/Cosine）、主键 / 自动 ID、多向量字段、JSON / 数组等高级类型Milvus
3. 索引与检索：ANN 算法选型（FLAT、IVF 系列、HNSW、DISKANN）、索引参数调优、混合搜索、过滤 / 范围 / 重排
4. 数据操作全链路：插入 / Upsert / 删除、批量导入、分区策略、Compaction、TTL、数据一致性与 WALMilvus
5. 运维与部署：单机 / 集群部署、K8s 与容器化、监控告警、性能调优（内存 / CPU/IO）、高可用与故障恢复
6. 治理与合规：元数据管理、数据质量校验、访问控制、审计日志、多租户隔离Milvus
7. 集成与开发：PyMilvus/Go/Java/C++ SDK、REST API、与 Embedding 模型 / ETL / 数据中台对接Milvus
8. 场景化实践：RAG 知识库、推荐系统、图像检索、多模态检索、实时检索链路建设
---

# 一、精通 MySQL 数据库设计与治理

## 1）数据库设计（必须熟练）

- 能独立设计**业务库表结构**：订单、用户、商品、日志等
- 懂三大范式、反范式、适度冗余
- 会建索引：普通索引、唯一索引、联合索引、最左匹配
- 会分表分库思路（水平 / 垂直拆分）
- 懂事务、隔离级别、锁机制（行锁、表锁、间隙锁）
- 能看懂执行计划 `EXPLAIN`，能优化慢查询

## 2）MySQL 治理（写 “精通” 就要会）

- 数据质量监控：主键唯一、非空、格式规范、重复数据治理
- 数据字典、表命名规范、字段规范
- 数据归档、冷热数据分离
- 权限管理、数据脱敏
- 主从复制、高可用基础概念
- 能处理常见问题：死锁、慢查询、连接数满、磁盘满

## 一句话底线

> 给你一个业务需求，你能**从 0 设计一套稳定、可扩展、易维护的 MySQL 库表**，并能长期治理数据质量。

---

# 二、Hive 数仓建模与分布式数据处理

## 1）数仓建模（核心硬技能）

- 熟练**数仓分层**：
    
    ODS → DWD → DWS → ADS / DIM
- 会主流建模方法：
    
    - 维度建模（星型模型、雪花模型）
    - 一致性维度、缓慢变化维 SCD
    
- 能设计事实表、维度表
- 懂拉链表、全量快照、增量同步

## 2）Hive SQL & 分布式处理

- 熟练 Hive SQL：开窗函数、分组、join、优化
- 懂数据倾斜及处理：key 打散、加盐、mapjoin
- 分区表、分桶表、压缩格式（ORC/Parquet）
- 了解 MR / Tez / Spark 执行原理
- 能处理 TB 级数据，保证任务稳定跑

## 一句话底线

> 能独立搭建**一套完整数仓模型**，从原始数据到指标产出全链路，且任务不倾斜、不卡死。

---

# 三、掌握向量数据库 Milvus

## 1）必须会的基础

- 理解向量 embedding 是什么（文本 / 图像 / 音频特征）
- 会搭建 / 连接 Milvus，建集合、建索引
- 做向量检索：近似最近邻 ANN 查询
- 会插入、删除、更新向量数据
- 会做相似度检索、过滤条件检索

## 2）加分项（写 “掌握” 最好会）

- 不同索引选型：IVF_FLAT、IVF_SQ8、HNSW 等
- 召回率、查询性能调优
- 结合 Python 对接模型（如 BERT 生成向量）
- 向量库 + 业务库联合查询

## 一句话底线

> 能把 embedding 存入 Milvus，并实现**高效相似检索**，能做检索系统、推荐、RAG 知识库。

---

# 综合：你写这句话，面试官默认你能做什么？

- 独立负责**业务库设计 + 数仓建模全流程**
- 能做数据治理、数据标准、数据质量
- 能处理海量离线数据，懂分布式优化
- 懂 AI 数据基础设施，能做向量检索 / RAG / 推荐相关开发