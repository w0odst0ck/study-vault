
## 一、Pandas 高频基础手撕（6 题）

### 手撕 1.给定 df，**删除全为空值的行和全为空值的列**，手写代码。
import pandas as pd
df = []
df = df.dropna(how='all')
df = df.dropna(axis=1)

### 手撕 2.手写代码：按 `dept` 分组，求 `salary` 均值、最大值、最小值，并重命名列。



### 手撕 3.手写：将 `create_time` 字符串列转为日期格式，**提取年、月、日**新增三列。


### 手撕 4.手写：筛选 **年龄 20-35 岁、城市为北京 / 上海、薪资大于 8000** 的数据。



### 手撕 5.手写：对 `score` 列做分箱：60 不及格、60-80 及格、80 优秀，新增 `level` 列。

### 手撕 6.手写：替换列中异常值，将 `salary` 小于 0 或大于 50000 的值改为中位数。


## 二、Pandas 数据清洗综合手撕（2 题）

### 手撕 7 经典数据治理清洗
'''
需求：

1. 按 `user_id` 去重，保留最新 `login_time`

2. 性别缺失用众数填充，薪资缺失用中位数填充

3. 过滤 `age` 不在 18-65 之间的异常行

4. 重置行索引
'''


### 手撕 8 批量字段类型转换
'''
需求：
把 `id、order_num` 转为字符串；
把 `amount、num` 转为浮点；
把所有时间字段统一转为 datetime。
'''


## 三、多表关联 ; 中台指标手撕（2 题）

### 手撕 9 两表关联宽表构建
'''
user 表：user_id, name, city
order 表：user_id, order_amt, order_dt

需求：

1. 左连接，保留全部用户

2. 统计每个用户订单总金额、订单次数

3. 无订单用户金额、次数填充为 0
'''


### 手撕 10 每日 UV、PV 统计
'''
行为表：dt, user_id, page_url
需求：按日期统计：每日访问人数 UV、访问次数 PV、独立页面数。
'''

## 四、Numpy 手撕代码（2 题）

### 手撕 11
'''
创建 5 行 4 列 随机整数数组（10-100），手写：

1. 每行最大值、每列最小值

2. 所有大于 50 的元素改为 0
'''



### 手撕 12.手写实现数组归一化到 [0,1]（按全局归一化）


## 五、Sklearn 特征工程手撕（2 题）

### 手撕 13.手写：数值列标准化、类别列 OneHot 编码，拼接特征。


### 手撕 14.手写：划分训练集测试集，测试集 20%，固定随机种子，分层抽样。

## 六、数据中台真实业务手撕真题（2 题）

### 手撕 15 数据质量校验手撕
'''
需求：手写函数，输入 df，输出：

- 每列缺失率

- 重复行数量

- 异常金额（amount;0）条数
'''


### 手撕 16 大表内存优化手撕:手写代码：对 df 做内存精简优化，把对象列转 category，数值列降精度。


## 1. 手撕：类别特征频率编码 ; 标签编码



## 2. 手撕：手写 TF-IDF 完整实现



## 3. 手撕：余弦相似度 + 向量 TopK 检索


## 4. 手撕：PCA 最简手写（纯 Numpy）


## 5. 手撕：数据集分层抽样（建模必用）


